/**
 * 基本問題① ステップ3
 * 猫クラス（Animal のサブクラス）
 * extends Animal、コンストラクタで super(name)、cry() をオーバーライドして「ニャー」と表示すること。
 */
public class Cat extends Animal {

    // コンストラクタ（super(name) を呼ぶ）
    public Cat(String name) {

    }

    // cry() をオーバーライド（「ニャー」と表示）
    @Override
    public void cry() {

    }
}

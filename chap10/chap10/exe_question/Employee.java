/**
 * 基本問題② ステップ1
 * 社員クラス（スーパークラス）
 * フィールド name, department、コンストラクタ、getName(), getDepartment()、introduce() で「〇〇です。〇〇部です。」を実装すること。
 */
public class Employee {

    // 社員の名前
    private String name;

    // 部署名
    private String department;

    // コンストラクタ：名前と部署を初期化
    public Employee(String name, String department) {

    }

    // getName()（getter）
    public String getName() {
        return null;
    }

    // getDepartment()（getter）
    public String getDepartment() {
        return null;
    }

    // 自己紹介を表示（「〇〇です。〇〇部です。」）
    public void introduce() {

    }
}

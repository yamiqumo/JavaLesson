/**
 * 基本問題② ステップ3
 * パートタイマークラス（Employee のサブクラス）
 * extends Employee、コンストラクタで super(name, department)、introduce() をオーバーライドして「〇〇です。パートで勤務しています。」と表示すること。
 */
public class PartTimer extends Employee {

    // コンストラクタ（super(name, department) を呼ぶ）
    public PartTimer(String name, String department) {

    }

    // introduce() をオーバーライド（「〇〇です。パートで勤務しています。」）
    @Override
    public void introduce() {

    }
}
